%Calculo aproximado da area de um circulo
app testeVII
    int r;
    real A;
    real pi := 3,1415;
    start
        read(r);

        A := 0;
        A := r * r * pi;
        
        write({A area do circulo e: });
        write(A);
    stop