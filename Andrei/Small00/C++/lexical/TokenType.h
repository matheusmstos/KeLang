#ifndef TOKENTYPE_H
#define TOKENTYPE_H

enum TokenType {
    // special tokens
    INVALID_TOKEN  = -2,
    UNEXPECTED_EOF = -1,
    END_OF_FILE    = 0,

    // symbols

    // keywords

    // operators

    // others

};

#endif
